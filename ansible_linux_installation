Ansinle installation in linux steps

1 st  = 
        curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

2 nd  =
        python3 get-pip.py --user

3rd  =
       python3 -m pip install --user ansible

4th  =
      ansible --version
